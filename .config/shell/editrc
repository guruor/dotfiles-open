# Use vi mode
bind -v

# Tab to complete
# bind \\t rl_complete

# Reverse search through history
bind "^R" em-inc-search-prev

# Clear the screen
bind "^L" ed-clear-screen

# LLDB specific config
lldb:bind -v
lldb:bind ^I lldb_complete
lldb:bind ^P ed-command
