{
  "Jira code SQL": {
    "prefix": "codesql",
    "body": [
      "```sql",
      "{color:green}[QUERY]{color}",
      "{code:sql}",
      "${0}",
      "{code}",
      "{color:red}[ROLLBACK]{color}",
      "{code:sql}",
      "{code}",
      "```"
    ],
    "description": "Jira code snippet for SQL"
  }
}
